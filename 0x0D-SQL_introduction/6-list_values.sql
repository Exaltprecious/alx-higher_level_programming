-- updates the score of Bob to 10 in the table named second_table.
UPDATE second_table
-- SELECT * FROM tableName -> List all the rows of ALL columns, * is a wildcard denoting all columns
SELECT * FROM first_table
